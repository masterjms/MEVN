import Home from "../views/Home.vue" 
const routes = [
    {
        path: "/",
        name: "Home",
        component: Home,
    }
] 
export default routes

// 홈라우트 . path, name, 라우트에 해당하는 컴포넌트를 설정.